import Link from 'next/link'
import { ArrowLeft, FileClock } from 'lucide-react'
export default async function ReportRunPage({params}:{params:Promise<{reportId:string}>}){const{reportId}=await params;return <div className="space-y-6"><Link href="/dashboard/intelligence/reports" className="inline-flex items-center gap-2 text-sm font-semibold text-gray-500"><ArrowLeft className="h-4 w-4"/>Reports</Link><div className="rounded-3xl border border-dashed border-gray-300 bg-white p-12 text-center dark:border-white/15 dark:bg-gray-900"><FileClock className="mx-auto h-7 w-7 text-gray-300"/><h1 className="mt-4 font-semibold">Report run {reportId.slice(0,10)}</h1><p className="mt-2 text-sm text-gray-500">Status and authorized artifact download appear after report generation is connected.</p></div></div>}
