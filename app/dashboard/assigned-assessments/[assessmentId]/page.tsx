import Link from 'next/link'
import { ArrowLeft, Calculator, CalendarRange, Clock3, EyeOff, FileQuestion, ShieldCheck, UserRound } from 'lucide-react'
export default async function AssignedAssessmentDetails({params}:{params:Promise<{assessmentId:string}>}){const {assessmentId}=await params;return <div className="mx-auto max-w-4xl space-y-6"><Link href="/dashboard/assigned-assessments" className="inline-flex items-center gap-2 text-sm font-semibold text-gray-500"><ArrowLeft className="h-4 w-4"/>Assigned assessments</Link><section className="rounded-3xl border border-gray-200 bg-white p-7 shadow-sm dark:border-white/10 dark:bg-gray-900"><span className="rounded-lg bg-blue-50 px-2 py-1 text-xs font-bold text-blue-700 dark:bg-blue-500/10">ASSESSMENT</span><h1 className="mt-5 text-3xl font-semibold">Assessment details</h1><p className="mt-2 text-sm text-gray-500">Review every condition before starting. Nothing begins until you confirm.</p><div className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{[[UserRound,'Teacher','Shown after assignment loads'],[Clock3,'Duration','Defined by teacher'],[FileQuestion,'Questions','Shown before starting'],[Calculator,'Allowed tools','Based on assessment settings'],[CalendarRange,'Availability','Shown in your timezone'],[EyeOff,'Results','Release policy shown here']].map(([Icon,label,value])=><div key={label as string} className="rounded-2xl bg-gray-50 p-4 dark:bg-white/5">{typeof Icon!=='string'&&<Icon className="h-4 w-4 text-gray-400"/>}<p className="mt-3 text-xs text-gray-400">{label as string}</p><p className="mt-1 text-sm font-semibold">{value as string}</p></div>)}</div><div className="mt-6 rounded-2xl border border-dashed border-gray-300 p-5 dark:border-white/15"><h2 className="text-sm font-semibold">Instructions and policies</h2><p className="mt-2 text-sm leading-6 text-gray-500">Instructions, marks, attempts, late policy, availability window, allowed tools, and result release rules will be loaded from the published assignment. No hidden conditions are introduced after you start.</p></div><div className="mt-6 flex items-center justify-between rounded-2xl bg-emerald-50 p-4 dark:bg-emerald-500/10"><div className="flex gap-3"><ShieldCheck className="h-5 w-5 text-emerald-600"/><div><p className="text-sm font-semibold text-emerald-900 dark:text-emerald-200">Your work is always safe</p><p className="mt-1 text-xs text-emerald-700 dark:text-emerald-300">Answers save automatically and retry after connection problems.</p></div></div><Link href={`/dashboard/assigned-assessments/${assessmentId}/check`} className="rounded-xl bg-gray-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-gray-950">Continue</Link></div></section></div>}
