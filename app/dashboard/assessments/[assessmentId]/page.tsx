import Link from 'next/link'
import { ArrowLeft, Clock3, FileQuestion, History, Settings, Users } from 'lucide-react'

const tabs = [{name:'Overview',icon:Clock3},{name:'Assigned Sections',icon:Users},{name:'Students',icon:Users},{name:'Questions',icon:FileQuestion},{name:'Settings',icon:Settings},{name:'History',icon:History}]
export default async function AssessmentDetailPage({ params }: { params: Promise<{ assessmentId: string }> }) { const { assessmentId } = await params; return <div className="space-y-6"><header className="flex flex-wrap items-center gap-4"><Link href="/dashboard/assessments" className="grid h-10 w-10 place-items-center rounded-xl border border-gray-200 dark:border-white/10"><ArrowLeft className="h-4 w-4" /></Link><div className="flex-1"><p className="text-xs font-bold uppercase tracking-wide text-blue-600">Draft assessment</p><h1 className="mt-1 text-2xl font-semibold">Assessment overview</h1></div><Link href={`/dashboard/assessments/${assessmentId}/edit`} className="rounded-xl bg-gray-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-gray-950">Continue editing</Link></header><nav className="flex gap-1 overflow-x-auto rounded-2xl border border-gray-200 bg-white p-2 dark:border-white/10 dark:bg-gray-900">{tabs.map(({name,icon:Icon},index)=><button key={name} className={`inline-flex shrink-0 items-center gap-2 rounded-xl px-3 py-2 text-xs font-semibold ${index===0?'bg-gray-950 text-white dark:bg-white dark:text-gray-950':'text-gray-500'}`}><Icon className="h-3.5 w-3.5"/>{name}</button>)}</nav><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Status','Draft'],['Questions','0'],['Total marks','0'],['Recipients','Not assigned']].map(([label,value])=><div key={label} className="rounded-3xl border border-gray-200 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-gray-900"><p className="text-xs text-gray-400">{label}</p><p className="mt-3 text-xl font-semibold">{value}</p></div>)}</div><div className="rounded-3xl border border-dashed border-gray-300 bg-white p-10 text-center dark:border-white/15 dark:bg-gray-900"><History className="mx-auto h-6 w-6 text-gray-300"/><p className="mt-3 text-sm font-semibold">No persisted assessment loaded</p><p className="mt-1 text-xs text-gray-400">Overview data and history will load through the tenant repository after migration deployment.</p></div></div> }
