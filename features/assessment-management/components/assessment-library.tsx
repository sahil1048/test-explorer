'use client'
import { useState } from 'react'
import Link from 'next/link'
import { Archive, CalendarClock, ClipboardCheck, Copy, FilePlus2, MoreHorizontal, Search } from 'lucide-react'
import { ASSESSMENT_STATUSES, type AssessmentStatus, type AssessmentSummary } from '@/features/assessment-builder/types'

export default function AssessmentLibrary({ assessments = [] }: { assessments?: AssessmentSummary[] }) {
  const [status, setStatus] = useState<AssessmentStatus | 'all'>('all'); const [query, setQuery] = useState('')
  const visible = assessments.filter((item) => (status === 'all' || item.status === status) && item.title.toLowerCase().includes(query.toLowerCase()))
  return <div className="space-y-7"><header className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-600">Assessment workspace</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Assessments</h1><p className="mt-2 text-sm text-gray-500">Build, assign, schedule, and manage school assessments.</p></div><div className="flex gap-2"><Link href="/dashboard/assessments/templates" className="inline-flex h-10 items-center gap-2 rounded-xl border border-gray-200 bg-white px-4 text-sm font-semibold dark:border-white/10 dark:bg-gray-900"><Copy className="h-4 w-4" /> Templates</Link><Link href="/dashboard/assessments/new" className="inline-flex h-10 items-center gap-2 rounded-xl bg-gray-950 px-4 text-sm font-semibold text-white dark:bg-white dark:text-gray-950"><FilePlus2 className="h-4 w-4" /> Create assessment</Link></div></header><div className="rounded-3xl border border-gray-200/80 bg-white p-4 shadow-sm dark:border-white/10 dark:bg-gray-900"><div className="flex flex-col gap-3 lg:flex-row lg:items-center"><div className="flex gap-1 overflow-x-auto">{(['all',...ASSESSMENT_STATUSES] as const).map((item) => <button key={item} onClick={() => setStatus(item)} className={`shrink-0 rounded-xl px-3 py-2 text-xs font-semibold capitalize ${status === item ? 'bg-gray-950 text-white dark:bg-white dark:text-gray-950' : 'text-gray-500 hover:bg-gray-50 dark:hover:bg-white/5'}`}>{item}</button>)}</div><label className="relative ml-auto w-full lg:w-72"><Search className="absolute left-3 top-3 h-4 w-4 text-gray-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search assessments…" className="h-10 w-full rounded-xl border border-gray-200 bg-gray-50 pl-9 pr-3 text-sm outline-none dark:border-white/10 dark:bg-white/5" /></label></div></div>{visible.length ? <div className="overflow-hidden rounded-3xl border border-gray-200 bg-white dark:border-white/10 dark:bg-gray-900"><table className="w-full min-w-[850px] text-left text-sm"><thead><tr className="border-b border-gray-100 text-xs text-gray-400 dark:border-white/10"><th className="p-4">Assessment</th><th>Class</th><th>Questions</th><th>Marks</th><th>Status</th><th>Updated</th><th /></tr></thead><tbody>{visible.map((item) => <tr key={item.id} className="border-b border-gray-100 last:border-0 dark:border-white/10"><td className="p-4"><Link href={`/dashboard/assessments/${item.id}`} className="font-semibold hover:text-blue-600">{item.title}</Link><p className="mt-1 text-xs text-gray-400">{item.type} · {item.subject}</p></td><td>{item.grade} · {item.sections.join(', ')}</td><td>{item.questions}</td><td>{item.totalMarks}</td><td><span className="rounded-lg bg-gray-100 px-2 py-1 text-[10px] font-bold uppercase dark:bg-white/5">{item.status}</span></td><td className="text-xs text-gray-400">{item.updatedAt}</td><td><MoreHorizontal className="h-4 w-4" /></td></tr>)}</tbody></table></div> : <div className="rounded-3xl border border-dashed border-gray-300 bg-white px-6 py-16 text-center dark:border-white/15 dark:bg-gray-900"><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600 dark:bg-blue-500/10"><ClipboardCheck className="h-5 w-5" /></div><h2 className="mt-5 text-lg font-semibold">{query || status !== 'all' ? 'No matching assessments' : 'Create your first assessment'}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-gray-500">Start with basic details, build a balanced paper from the Question Bank, then assign it to students.</p><Link href="/dashboard/assessments/new" className="mt-6 inline-flex rounded-xl bg-gray-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-gray-950">Create assessment</Link></div>}<div className="grid gap-3 sm:grid-cols-3"><Info icon={CalendarClock} title="Schedule precisely" text="Control availability, due dates, attempts, and late submission." /><Info icon={Copy} title="Reuse safely" text="Duplicate assessments or save reusable blueprint templates." /><Info icon={Archive} title="Preserve history" text="Archive completed work without deleting institutional records." /></div></div>
}
function Info({ icon: Icon, title, text }: { icon: typeof Copy; title: string; text: string }) { return <div className="rounded-2xl bg-gray-100/60 p-4 dark:bg-white/5"><Icon className="h-4 w-4 text-gray-400" /><p className="mt-3 text-sm font-semibold">{title}</p><p className="mt-1 text-xs leading-5 text-gray-400">{text}</p></div> }
