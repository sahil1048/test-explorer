'use client'
import { useState } from 'react'
import { CalendarDays, ChevronLeft, ChevronRight } from 'lucide-react'
const views=['Month','Week','Agenda'] as const
export default function CalendarView(){const[view,setView]=useState<(typeof views)[number]>('Month');return <div className="space-y-7"><header><p className="text-xs font-bold uppercase tracking-wide text-blue-600">Academic schedule</p><h1 className="mt-3 text-3xl font-semibold">Calendar</h1><p className="mt-2 text-sm text-gray-500">Assessments, deadlines, school events, announcements, and future holidays.</p></header><div className="flex flex-wrap items-center gap-2 rounded-3xl border border-gray-200 bg-white p-3 dark:border-white/10 dark:bg-gray-900"><button className="grid h-9 w-9 place-items-center rounded-xl border border-gray-200 dark:border-white/10"><ChevronLeft className="h-4 w-4"/></button><button className="grid h-9 w-9 place-items-center rounded-xl border border-gray-200 dark:border-white/10"><ChevronRight className="h-4 w-4"/></button><span className="ml-2 flex-1 text-sm font-semibold">Selected period</span><div className="flex rounded-xl border border-gray-200 p-1 dark:border-white/10">{views.map(item=><button key={item} onClick={()=>setView(item)} className={`rounded-lg px-3 py-1.5 text-xs font-semibold ${view===item?'bg-gray-950 text-white dark:bg-white dark:text-gray-950':'text-gray-500'}`}>{item}</button>)}</div></div><div className="grid min-h-[420px] place-items-center rounded-3xl border border-dashed border-gray-300 bg-white p-10 text-center dark:border-white/15 dark:bg-gray-900"><div><CalendarDays className="mx-auto h-7 w-7 text-gray-300"/><h2 className="mt-4 font-semibold">No calendar entries</h2><p className="mt-2 text-sm text-gray-500">The {view.toLowerCase()} view will project real platform events without duplicating their source records.</p></div></div></div>}
