import Link from 'next/link'
import { ArrowRight, CalendarClock, CheckCircle2, ClipboardCheck, Megaphone, Plus, UsersRound } from 'lucide-react'
export default function TeacherOverview({profile}:{profile:{full_name?:string|null}}){return <div className="space-y-8"><header className="rounded-3xl border border-gray-200 bg-white p-7 shadow-sm dark:border-white/10 dark:bg-gray-900"><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-600">Teacher workspace</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Good to see you, {profile.full_name?.split(' ')[0]||'Teacher'}</h1><p className="mt-2 text-sm text-gray-500">Start with work that needs attention, then decide what your class needs next.</p><div className="mt-6 flex flex-wrap gap-2"><Action href="/dashboard/evaluation" icon={ClipboardCheck} label="Review submissions"/><Action href="/dashboard/assessments/new" icon={Plus} label="Create assessment"/><Action href="/dashboard/question-bank/new" icon={Plus} label="Create question"/></div></header><div className="grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><div className="space-y-6"><Section title="Today’s assessments" href="/dashboard/assessments"><Empty icon={CalendarClock} title="No assessments today" text="Scheduled and active assessments will appear here."/></Section><Section title="Submissions awaiting review" href="/dashboard/evaluation"><Empty icon={ClipboardCheck} title="Nothing awaiting review" text="Constructed responses appear here after real student submissions."/></Section><Section title="Classes requiring attention"><Empty icon={UsersRound} title="No evidence yet" text="Classes appear only when real performance evidence crosses a documented attention threshold."/></Section></div><div className="space-y-6"><Small title="Recently published results" icon={CheckCircle2} text="No results published recently."/><Small title="Upcoming assessments" icon={CalendarClock} text="No upcoming assessments."/><Small title="Recent activity" icon={ClipboardCheck} text="No evaluation activity recorded."/><Small title="Announcements" icon={Megaphone} text="No announcements to show."/></div></div></div>}
function Action({href,icon:Icon,label}:{href:string;icon:typeof Plus;label:string}){return <Link href={href} className="inline-flex h-10 items-center gap-2 rounded-xl bg-gray-950 px-4 text-sm font-semibold text-white first:bg-blue-600 dark:bg-white dark:text-gray-950"><Icon className="h-4 w-4"/>{label}</Link>}
function Section({title,href,children}:{title:string;href?:string;children:React.ReactNode}){return <section><div className="mb-3 flex justify-between"><h2 className="font-semibold">{title}</h2>{href&&<Link href={href} className="inline-flex items-center gap-1 text-xs font-semibold text-blue-600">View all<ArrowRight className="h-3.5 w-3.5"/></Link>}</div>{children}</section>}
function Empty({icon:Icon,title,text}:{icon:typeof Plus;title:string;text:string}){return <div className="rounded-3xl border border-dashed border-gray-300 bg-white p-9 text-center dark:border-white/15 dark:bg-gray-900"><Icon className="mx-auto h-6 w-6 text-gray-300"/><p className="mt-3 text-sm font-semibold">{title}</p><p className="mt-1 text-xs text-gray-400">{text}</p></div>}
function Small({title,icon:Icon,text}:{title:string;icon:typeof Plus;text:string}){return <section className="rounded-3xl border border-gray-200 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-gray-900"><div className="flex items-center gap-2"><Icon className="h-4 w-4 text-gray-400"/><h2 className="text-sm font-semibold">{title}</h2></div><p className="mt-3 text-xs leading-5 text-gray-400">{text}</p></section>}
