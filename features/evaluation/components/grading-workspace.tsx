'use client'
import { useEffect, useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, Check, ChevronLeft, ChevronRight, Command, Save } from 'lucide-react'
export default function GradingWorkspace({attemptId}:{attemptId:string}){const [score,setScore]=useState('');const [comment,setComment]=useState('');const [status,setStatus]=useState<'saved'|'saving'>('saved');useEffect(()=>{if(status!=='saving')return;const timer=window.setTimeout(()=>setStatus('saved'),600);return()=>window.clearTimeout(timer)},[score,comment,status]);const change=(fn:(value:string)=>void,value:string)=>{fn(value);setStatus('saving')};return <div className="space-y-5"><header className="flex flex-wrap items-center gap-3 rounded-3xl border border-gray-200 bg-white p-4 shadow-sm dark:border-white/10 dark:bg-gray-900"><Link href="/dashboard/evaluation" className="grid h-9 w-9 place-items-center rounded-xl border border-gray-200 dark:border-white/10"><ArrowLeft className="h-4 w-4"/></Link><div className="flex-1"><h1 className="font-semibold">Manual grading</h1><p className="mt-1 text-xs text-gray-400">Attempt {attemptId.slice(0,12)} · Response 1 of 1</p></div><span aria-live="polite" className="inline-flex items-center gap-1 text-xs text-gray-400">{status==='saving'?<Save className="h-3.5 w-3.5"/>:<Check className="h-3.5 w-3.5 text-emerald-500"/>}{status==='saving'?'Saving…':'Saved locally'}</span></header><div className="grid gap-5 xl:grid-cols-[1fr_380px]"><main className="space-y-5"><Panel title="Question"><p className="text-sm leading-7 text-gray-500">The published short-answer or essay question will appear here with its maximum marks and optional previous answer.</p></Panel><Panel title="Student answer"><div className="min-h-48 rounded-2xl bg-gray-50 p-5 text-sm leading-7 text-gray-500 dark:bg-white/5">No persisted response has been loaded.</div></Panel><Panel title="Teacher feedback"><textarea value={comment} onChange={e=>change(setComment,e.target.value)} rows={5} className="editor-input h-auto py-3" placeholder="Specific feedback the student can act on…"/><div className="grid gap-3 sm:grid-cols-2"><textarea rows={3} className="editor-input h-auto py-3" placeholder="Positive note…"/><textarea rows={3} className="editor-input h-auto py-3" placeholder="Improvement suggestion…"/></div><textarea rows={2} className="editor-input h-auto py-3" placeholder="Private teacher note — never shown to student"/></Panel></main><aside className="space-y-5"><Panel title="Rubric"><p className="text-xs leading-5 text-gray-400">Assign a reusable rubric to score criteria consistently. No rubric is assigned.</p><Link href="/dashboard/rubrics" className="mt-4 inline-flex text-xs font-semibold text-blue-600">Choose or create rubric</Link></Panel><Panel title="Score"><label className="text-xs font-semibold">Teacher score<input value={score} onChange={e=>change(setScore,e.target.value)} type="number" min="0" className="editor-input mt-2" placeholder="0"/></label><button className="mt-4 w-full rounded-xl bg-gray-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-gray-950">Complete review</button></Panel><div className="rounded-2xl bg-gray-100 p-4 text-xs leading-5 text-gray-500 dark:bg-white/5"><Command className="mr-1 inline h-3.5 w-3.5"/>Shortcuts: save, previous/next response, and score focus connect with persistence.</div></aside></div><footer className="flex justify-between"><button className="inline-flex items-center gap-2 rounded-xl border border-gray-200 px-4 py-2 text-sm font-semibold dark:border-white/10"><ChevronLeft className="h-4 w-4"/>Previous student</button><button className="inline-flex items-center gap-2 rounded-xl bg-gray-950 px-4 py-2 text-sm font-semibold text-white dark:bg-white dark:text-gray-950">Next student<ChevronRight className="h-4 w-4"/></button></footer></div>}
function Panel({title,children}:{title:string;children:React.ReactNode}){return <section className="rounded-3xl border border-gray-200 bg-white p-6 shadow-sm dark:border-white/10 dark:bg-gray-900"><h2 className="mb-4 font-semibold">{title}</h2><div className="space-y-4">{children}</div></section>}
