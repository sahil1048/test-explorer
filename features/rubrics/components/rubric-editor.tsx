'use client'
import { useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, GripVertical, Plus, Trash2 } from 'lucide-react'
export default function RubricEditor({rubricId}:{rubricId?:string}){const [criteria,setCriteria]=useState([{id:1,name:'',description:'',marks:'5'}]);return <div className="mx-auto max-w-4xl space-y-6"><header className="flex items-center gap-4"><Link href="/dashboard/rubrics" className="grid h-10 w-10 place-items-center rounded-xl border border-gray-200 dark:border-white/10"><ArrowLeft className="h-4 w-4"/></Link><div className="flex-1"><p className="text-xs font-bold uppercase tracking-wide text-blue-600">Rubrics</p><h1 className="mt-1 text-2xl font-semibold">{rubricId?'Edit rubric':'Create rubric'}</h1></div><button className="rounded-xl bg-gray-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-gray-950">Save rubric</button></header><section className="space-y-5 rounded-3xl border border-gray-200 bg-white p-6 shadow-sm dark:border-white/10 dark:bg-gray-900"><label className="text-xs font-semibold">Rubric name<input className="editor-input mt-2" placeholder="Scientific explanation rubric"/></label><label className="text-xs font-semibold">Description<textarea className="editor-input mt-2 h-auto py-3" rows={3}/></label><label className="text-xs font-semibold">Visibility<select className="editor-input mt-2"><option>Organization</option><option>Private</option></select></label></section><section className="rounded-3xl border border-gray-200 bg-white p-6 shadow-sm dark:border-white/10 dark:bg-gray-900"><div className="flex justify-between"><div><h2 className="font-semibold">Criteria</h2><p className="mt-1 text-xs text-gray-400">Maximum marks across criteria define the rubric total.</p></div><span className="text-sm font-semibold">{criteria.reduce((sum,item)=>sum+(Number(item.marks)||0),0)} marks</span></div><div className="mt-5 space-y-3">{criteria.map((item,index)=><div key={item.id} className="grid gap-3 rounded-2xl border border-gray-200 p-4 dark:border-white/10 sm:grid-cols-[auto_1fr_110px_auto]"><GripVertical className="mt-3 h-4 w-4 text-gray-300"/><div className="space-y-2"><input value={item.name} onChange={e=>setCriteria(rows=>rows.map(row=>row.id===item.id?{...row,name:e.target.value}:row))} className="editor-input" placeholder={`Criterion ${index+1}`}/><textarea value={item.description} onChange={e=>setCriteria(rows=>rows.map(row=>row.id===item.id?{...row,description:e.target.value}:row))} className="editor-input h-auto py-3" rows={2} placeholder="What demonstrates success?"/></div><input value={item.marks} onChange={e=>setCriteria(rows=>rows.map(row=>row.id===item.id?{...row,marks:e.target.value}:row))} type="number" min="0" className="editor-input"/><button aria-label="Remove criterion" onClick={()=>setCriteria(rows=>rows.filter(row=>row.id!==item.id))}><Trash2 className="h-4 w-4 text-gray-400"/></button></div>)}</div><button onClick={()=>setCriteria(rows=>[...rows,{id:Date.now(),name:'',description:'',marks:'1'}])} className="mt-4 inline-flex items-center gap-2 text-sm font-semibold text-blue-600"><Plus className="h-4 w-4"/>Add criterion</button></section></div>}
