'use client'

import { useMemo, useState } from 'react'
import Link from 'next/link'
import { Archive, ChevronDown, Copy, Filter, Grid2X2, List, MoreHorizontal, Search, Trash2 } from 'lucide-react'
import type { QuestionSummary } from '@/features/question-bank/types'

const filters = ['Grade', 'Subject', 'Chapter', 'Topic', 'Difficulty', 'Question type', 'Status', 'Bloom level', 'Tags', 'Author', 'Updated']

export default function QuestionLibrary({ questions = [] }: { questions?: QuestionSummary[] }) {
  const [query, setQuery] = useState('')
  const [view, setView] = useState<'table' | 'grid'>('table')
  const [selected, setSelected] = useState<string[]>([])
  const visible = useMemo(() => questions.filter((question) => `${question.title} ${question.bodyText} ${question.subject}`.toLowerCase().includes(query.toLowerCase())), [questions, query])

  return <div className="space-y-5"><div className="rounded-3xl border border-gray-200/80 bg-white p-4 shadow-sm dark:border-white/10 dark:bg-gray-900"><div className="flex flex-col gap-3 lg:flex-row"><label className="relative flex-1"><Search className="absolute left-3.5 top-3 h-4 w-4 text-gray-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search question text, title, tags…" className="h-10 w-full rounded-xl border border-gray-200 bg-gray-50 pl-10 pr-4 text-sm outline-none transition focus:border-blue-400 focus:ring-2 focus:ring-blue-100 dark:border-white/10 dark:bg-white/5 dark:focus:ring-blue-500/20" /></label><div className="flex gap-2"><button className="inline-flex h-10 items-center gap-2 rounded-xl border border-gray-200 px-3 text-sm font-semibold text-gray-600 dark:border-white/10 dark:text-gray-300"><Filter className="h-4 w-4" /> Filters</button><div className="flex rounded-xl border border-gray-200 p-1 dark:border-white/10"><button aria-label="Table view" onClick={() => setView('table')} className={`grid h-8 w-8 place-items-center rounded-lg ${view === 'table' ? 'bg-gray-100 dark:bg-white/10' : ''}`}><List className="h-4 w-4" /></button><button aria-label="Grid view" onClick={() => setView('grid')} className={`grid h-8 w-8 place-items-center rounded-lg ${view === 'grid' ? 'bg-gray-100 dark:bg-white/10' : ''}`}><Grid2X2 className="h-4 w-4" /></button></div></div></div><div className="mt-3 flex gap-2 overflow-x-auto pb-1">{filters.map((filter) => <button key={filter} className="inline-flex shrink-0 items-center gap-1 rounded-lg bg-gray-50 px-2.5 py-1.5 text-xs font-medium text-gray-500 dark:bg-white/5 dark:text-gray-400">{filter}<ChevronDown className="h-3 w-3" /></button>)}</div></div>

  {selected.length > 0 && <div className="flex flex-wrap items-center gap-2 rounded-2xl bg-gray-950 p-3 text-white dark:bg-white dark:text-gray-950"><span className="mr-auto text-sm font-semibold">{selected.length} selected</span><Bulk icon={Copy} label="Duplicate" /><Bulk icon={Archive} label="Archive" /><Bulk icon={Trash2} label="Delete" /></div>}

  {!visible.length ? <div className="rounded-3xl border border-dashed border-gray-300 bg-white px-6 py-16 text-center dark:border-white/15 dark:bg-gray-900"><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600 dark:bg-blue-500/10 dark:text-blue-300"><Search className="h-5 w-5" /></div><h2 className="mt-5 text-lg font-semibold text-gray-950 dark:text-white">{query ? 'No matching questions' : 'Build your school question library'}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-gray-500 dark:text-gray-400">{query ? 'Change the search or clear filters.' : 'Create a question manually or import an existing bank. Questions begin as drafts.'}</p><div className="mt-6 flex justify-center gap-2"><Link href="/dashboard/question-bank/import" className="rounded-xl border border-gray-200 px-4 py-2.5 text-sm font-semibold dark:border-white/10">Import questions</Link><Link href="/dashboard/question-bank/new" className="rounded-xl bg-gray-950 px-4 py-2.5 text-sm font-semibold text-white dark:bg-white dark:text-gray-950">Create question</Link></div></div> : view === 'table' ? <div className="overflow-hidden rounded-3xl border border-gray-200/80 bg-white shadow-sm dark:border-white/10 dark:bg-gray-900"><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left"><thead className="border-b border-gray-100 bg-gray-50/70 text-[11px] uppercase tracking-wide text-gray-400 dark:border-white/10 dark:bg-white/[0.03]"><tr><th className="p-4"><input type="checkbox" aria-label="Select all" /></th><th>Question</th><th>Academic context</th><th>Type</th><th>Difficulty</th><th>Status</th><th>Updated</th><th /></tr></thead><tbody>{visible.map((question) => <tr key={question.id} className="border-b border-gray-100 text-sm last:border-0 dark:border-white/10"><td className="p-4"><input type="checkbox" checked={selected.includes(question.id)} onChange={() => setSelected((current) => current.includes(question.id) ? current.filter((id) => id !== question.id) : [...current, question.id])} /></td><td className="max-w-sm py-4 pr-5"><Link href={`/dashboard/question-bank/${question.id}`} className="font-semibold text-gray-900 hover:text-blue-600 dark:text-white">{question.title}</Link><p className="mt-1 truncate text-xs text-gray-400">{question.bodyText}</p></td><td className="text-xs text-gray-500">{question.grade} · {question.subject}</td><td className="text-xs capitalize text-gray-500">{question.type.replaceAll('_', ' ')}</td><td><Pill value={question.difficulty} /></td><td><Pill value={question.status} /></td><td className="text-xs text-gray-400">{question.updatedAt}</td><td><button aria-label="Question actions"><MoreHorizontal className="h-4 w-4 text-gray-400" /></button></td></tr>)}</tbody></table></div></div> : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{visible.map((question) => <Link key={question.id} href={`/dashboard/question-bank/${question.id}`} className="rounded-3xl border border-gray-200 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-gray-900"><div className="flex justify-between"><Pill value={question.status} /><Pill value={question.difficulty} /></div><h2 className="mt-5 font-semibold">{question.title}</h2><p className="mt-2 line-clamp-3 text-sm text-gray-500">{question.bodyText}</p><p className="mt-5 text-xs text-gray-400">{question.grade} · {question.subject}</p></Link>)}</div>}</div>
}

function Pill({ value }: { value: string }) { return <span className="rounded-lg bg-gray-100 px-2 py-1 text-[10px] font-bold capitalize text-gray-500 dark:bg-white/5 dark:text-gray-300">{value}</span> }
function Bulk({ icon: Icon, label }: { icon: typeof Copy; label: string }) { return <button className="inline-flex items-center gap-1.5 rounded-lg bg-white/10 px-3 py-1.5 text-xs font-semibold dark:bg-gray-950/10"><Icon className="h-3.5 w-3.5" />{label}</button> }
