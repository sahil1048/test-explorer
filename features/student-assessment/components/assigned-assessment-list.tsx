'use client'
import { useState } from 'react'
import Link from 'next/link'
import { CalendarClock, ClipboardList, Search } from 'lucide-react'
import type { AssignedAssessmentSummary } from '@/features/student-assessment/types'
const statuses = ['all','upcoming','active','completed','overdue'] as const
export default function AssignedAssessmentList({ assessments=[] }: { assessments?:AssignedAssessmentSummary[] }) { const [status,setStatus]=useState<(typeof statuses)[number]>('all'); const [query,setQuery]=useState(''); const visible=assessments.filter(a=>(status==='all'||a.status===status)&&`${a.title} ${a.subject} ${a.teacher}`.toLowerCase().includes(query.toLowerCase())); return <div className="space-y-7"><header><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-600">My work</p><h1 className="mt-3 text-3xl font-semibold tracking-tight">Assigned assessments</h1><p className="mt-2 text-sm text-gray-500">Everything assigned to you, with clear deadlines and attempt status.</p></header><div className="rounded-3xl border border-gray-200 bg-white p-4 dark:border-white/10 dark:bg-gray-900"><div className="flex flex-col gap-3 sm:flex-row"><div className="flex gap-1 overflow-x-auto">{statuses.map(item=><button key={item} onClick={()=>setStatus(item)} className={`shrink-0 rounded-xl px-3 py-2 text-xs font-semibold capitalize ${status===item?'bg-gray-950 text-white dark:bg-white dark:text-gray-950':'text-gray-500'}`}>{item}</button>)}</div><label className="relative ml-auto w-full sm:w-72"><Search className="absolute left-3 top-3 h-4 w-4 text-gray-400"/><input value={query} onChange={e=>setQuery(e.target.value)} className="editor-input pl-9" placeholder="Search assignments…"/></label></div></div>{visible.length?<div className="grid gap-4 lg:grid-cols-2">{visible.map(item=><Link key={item.id} href={`/dashboard/assigned-assessments/${item.id}`} className="rounded-3xl border border-gray-200 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-gray-900"><div className="flex items-start justify-between"><span className="rounded-lg bg-blue-50 px-2 py-1 text-[10px] font-bold uppercase text-blue-700 dark:bg-blue-500/10 dark:text-blue-200">{item.status}</span><span className="text-xs text-gray-400">{item.attemptsRemaining} attempts left</span></div><h2 className="mt-5 font-semibold">{item.title}</h2><p className="mt-1 text-xs text-gray-400">{item.subject} · {item.className} · {item.teacher}</p><div className="mt-5 flex justify-between text-xs text-gray-500"><span>{item.durationMinutes} minutes</span><span>{item.dueAt||'No due date'}</span></div><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-gray-100 dark:bg-white/10"><div className="h-full bg-blue-600" style={{width:`${item.progress}%`}}/></div></Link>)}</div>:<div className="rounded-3xl border border-dashed border-gray-300 bg-white px-6 py-16 text-center dark:border-white/15 dark:bg-gray-900"><ClipboardList className="mx-auto h-7 w-7 text-gray-300"/><h2 className="mt-4 font-semibold">{query||status!=='all'?'No matching assessments':'No assessments assigned'}</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-gray-500">When a teacher publishes work for your section, it will appear here with its availability and due date.</p></div>}<div className="rounded-2xl bg-blue-50 p-4 text-sm text-blue-800 dark:bg-blue-500/10 dark:text-blue-200"><CalendarClock className="mr-2 inline h-4 w-4"/>Starting an assessment creates a recoverable attempt. Your answers are saved as you work.</div></div> }
