'use client'

import { useState } from 'react'
import Link from 'next/link'
import { ArrowLeft, ArrowRight, Check, FileSpreadsheet, FileText, Upload } from 'lucide-react'

const steps = ['Format', 'Academic context', 'Upload', 'Validate', 'Preview', 'Import']

export default function ImportWizard() {
  const [step, setStep] = useState(1)
  const [format, setFormat] = useState<'csv' | 'docx'>('csv')
  return <div className="mx-auto max-w-5xl space-y-6"><header className="flex items-center gap-4"><Link href="/dashboard/question-bank" className="grid h-10 w-10 place-items-center rounded-xl border border-gray-200 dark:border-white/10"><ArrowLeft className="h-4 w-4" /></Link><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-600">Question Bank</p><h1 className="mt-1 text-2xl font-semibold">Bulk import</h1></div></header><div className="grid gap-6 lg:grid-cols-[220px_1fr]"><aside className="rounded-3xl border border-gray-200 bg-white p-3 dark:border-white/10 dark:bg-gray-900">{steps.map((label,index) => <div key={label} className={`flex items-center gap-3 rounded-2xl px-3 py-3 text-sm font-semibold ${index + 1 === step ? 'bg-gray-950 text-white dark:bg-white dark:text-gray-950' : 'text-gray-400'}`}><span className="grid h-7 w-7 place-items-center rounded-lg bg-gray-100/10 text-xs">{index + 1 < step ? <Check className="h-3.5 w-3.5" /> : index + 1}</span>{label}</div>)}</aside><main className="rounded-3xl border border-gray-200 bg-white p-7 shadow-sm dark:border-white/10 dark:bg-gray-900"><p className="text-xs font-bold uppercase tracking-widest text-gray-400">Step {step} of 6</p>{step === 1 && <section><h2 className="mt-4 text-2xl font-semibold">Choose an import format</h2><p className="mt-2 text-sm text-gray-500">Imported questions remain drafts until reviewed.</p><div className="mt-7 grid gap-3 sm:grid-cols-3"><FormatCard active={format === 'csv'} onClick={() => setFormat('csv')} icon={FileSpreadsheet} title="CSV" description="Structured bulk import" /><FormatCard active={format === 'docx'} onClick={() => setFormat('docx')} icon={FileText} title="Word (.docx)" description="Document parser" /><FormatCard disabled icon={FileText} title="PDF" description="Planned — not available" /></div></section>}{step === 2 && <Step title="Set academic defaults" description="Choose the year, grade, subject, and optional chapter applied to imported rows."><div className="grid gap-3 sm:grid-cols-2">{['Academic year','Grade','Subject','Chapter (optional)'].map((label) => <label key={label} className="text-xs font-semibold">{label}<select className="editor-input mt-2"><option>Select {label.toLowerCase()}</option></select></label>)}</div></Step>}{step === 3 && <Step title={`Upload your ${format.toUpperCase()} file`} description="The file is parsed into a temporary validation batch before any questions are created."><div className="rounded-2xl border border-dashed border-gray-300 p-12 text-center dark:border-white/15"><Upload className="mx-auto h-7 w-7 text-gray-300" /><p className="mt-3 text-sm font-semibold">Drop file here or browse</p><p className="mt-1 text-xs text-gray-400">No file has been uploaded.</p></div></Step>}{step >= 4 && <Step title={steps[step - 1]} description="Parsing and persistent import processing will activate after the migration and server actions are deployed."><div className="rounded-2xl bg-blue-50 p-5 text-sm leading-6 text-blue-800 dark:bg-blue-500/10 dark:text-blue-200">This stage will show row-level validation, mapped answers, warnings, and downloadable errors. It never imports invalid rows silently.</div></Step>}<div className="mt-10 flex justify-between border-t border-gray-100 pt-5 dark:border-white/10"><button disabled={step === 1} onClick={() => setStep(step - 1)} className="inline-flex items-center gap-2 rounded-xl px-4 py-2 text-sm font-semibold disabled:opacity-30"><ArrowLeft className="h-4 w-4" />Back</button><button disabled={step === 6} onClick={() => setStep(step + 1)} className="inline-flex items-center gap-2 rounded-xl bg-gray-950 px-4 py-2 text-sm font-semibold text-white disabled:opacity-30 dark:bg-white dark:text-gray-950">Continue<ArrowRight className="h-4 w-4" /></button></div></main></div></div>
}
function FormatCard({ icon: Icon, title, description, active, disabled, onClick }: { icon: typeof FileText; title: string; description: string; active?: boolean; disabled?: boolean; onClick?: () => void }) { return <button disabled={disabled} onClick={onClick} className={`rounded-2xl border p-5 text-left transition disabled:opacity-50 ${active ? 'border-blue-500 bg-blue-50 dark:bg-blue-500/10' : 'border-gray-200 dark:border-white/10'}`}><Icon className="h-5 w-5 text-blue-600" /><p className="mt-4 text-sm font-semibold">{title}</p><p className="mt-1 text-xs text-gray-400">{description}</p></button> }
function Step({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <section><h2 className="mt-4 text-2xl font-semibold">{title}</h2><p className="mt-2 text-sm text-gray-500">{description}</p><div className="mt-7">{children}</div></section> }
