import { AlertTriangle, CheckSquare, Filter, UserRoundPlus } from 'lucide-react'
export default function ActionCenter({mode}:{mode:'risks'|'interventions'}){const isRisk=mode==='risks';return <div className="space-y-7"><header><p className="text-xs font-bold uppercase tracking-wide text-blue-600">Decision queue</p><h1 className="mt-3 text-3xl font-semibold">{isRisk?'Student risk':'Intervention center'}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-gray-500">{isRisk?'Deterministic flags from participation, scores, repeated weak topics, missing work, and late submissions.':'Turn evidence into owned follow-up tasks with a due date and clear resolution.'}</p></header><div className="flex gap-2 overflow-x-auto rounded-3xl border border-gray-200 bg-white p-3 dark:border-white/10 dark:bg-gray-900"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-gray-100 dark:bg-white/5"><Filter className="h-4 w-4 text-gray-400"/></span>{(isRisk?['Status','Severity','Rule','Grade','Section']:['Status','Priority','Owner','Due date','Entity']).map(item=><button key={item} className="h-9 shrink-0 rounded-xl border border-gray-200 px-3 text-xs font-semibold text-gray-500 dark:border-white/10">{item}</button>)}</div><div className="rounded-3xl border border-dashed border-gray-300 bg-white p-12 text-center dark:border-white/15 dark:bg-gray-900">{isRisk?<AlertTriangle className="mx-auto h-7 w-7 text-gray-300"/>:<CheckSquare className="mx-auto h-7 w-7 text-gray-300"/>}<h2 className="mt-4 font-semibold">{isRisk?'No student risk flags':'No interventions assigned'}</h2><p className="mx-auto mt-2 max-w-lg text-sm leading-6 text-gray-500">{isRisk?'Flags appear only when a configured rule has persisted evidence. No predictive risk is inferred.':'Examples such as declining performance, overdue assessments, low participation, and delayed reviews require evidence before becoming actions.'}</p>{!isRisk&&<button disabled className="mt-5 inline-flex items-center gap-2 rounded-xl bg-gray-950 px-4 py-2 text-sm font-semibold text-white opacity-40 dark:bg-white dark:text-gray-950"><UserRoundPlus className="h-4 w-4"/>Assign follow-up</button>}</div></div>}
