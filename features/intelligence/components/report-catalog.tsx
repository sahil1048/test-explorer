import { Download, FileBarChart, FileText } from 'lucide-react'
const reports=['School Summary','Grade Summary','Section Summary','Subject Summary','Teacher Summary','Student Summary']
export default function ReportCatalog(){return <div className="space-y-7"><header><p className="text-xs font-bold uppercase tracking-wide text-blue-600">Snapshot-consistent exports</p><h1 className="mt-3 text-3xl font-semibold">Reports</h1><p className="mt-2 text-sm text-gray-500">Every report records filters, definition version, and source cutoff for reproducibility.</p></header><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{reports.map(name=><div key={name} className="rounded-3xl border border-gray-200 bg-white p-6 shadow-sm dark:border-white/10 dark:bg-gray-900"><FileBarChart className="h-5 w-5 text-blue-600"/><h2 className="mt-5 font-semibold">{name}</h2><p className="mt-2 text-sm leading-6 text-gray-500">Generate from the same evidence used by the command center.</p><div className="mt-5 flex gap-2"><button disabled className="inline-flex items-center gap-1 rounded-lg border border-gray-200 px-3 py-2 text-xs font-semibold opacity-50 dark:border-white/10"><Download className="h-3.5 w-3.5"/>CSV</button><button disabled className="inline-flex items-center gap-1 rounded-lg border border-gray-200 px-3 py-2 text-xs font-semibold opacity-50 dark:border-white/10"><FileText className="h-3.5 w-3.5"/>PDF</button></div></div>)}</div><div className="rounded-3xl border border-dashed border-gray-300 p-8 text-center dark:border-white/15"><p className="text-sm font-semibold">No report runs</p><p className="mt-1 text-xs text-gray-400">Queued, processing, completed, failed, and expired jobs will appear here.</p></div></div>}
